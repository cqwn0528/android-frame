package com.android.frame_master.base.recycle;

import android.content.Context;
import android.widget.TextView;

import com.android.frame_master.R;
import com.android.frame_master.bean.UserBean;

import java.util.List;

/*
public class ExampleAdapter extends BaseRecyclerAdapter<UserBean> {

    public ExampleAdapter(Context context, List<UserBean> data) {
        super(context, data);
    }

    @Override
    public void bindData(BaseViewHolder holder, UserBean record) {
        TextView tvId = (TextView) holder.getView(R.id.tvId);
        TextView tvRemark = (TextView) holder.getView(R.id.tvRemark);
        TextView tvCreatedAt = (TextView) holder.getView(R.id.tvCreatedAt);

        tvId.setText(record.getId().toString());
        tvRemark.setText(record.getRemark());
        tvCreatedAt.setText(record.getCreatedAt().toString());
    }

    @Override
    public int getItemLayoutId() {
        return R.layout.item_record;
    }
}*/
